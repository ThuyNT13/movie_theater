<div>

<% if @screen.errors.any? %>
  <div>
    <h2>
      <%= pluralize(@screen.errrors.count, "error") %> prohibited
      this article from being saved:
    </h2>

    <ul>
      <% @screen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

</div>
